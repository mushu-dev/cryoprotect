# CryoProtect Analyzer API Requirements (Updated for 2025)

# Flask and extensions
flask==3.0.2
flask-restful==0.3.10
flask-cors==4.0.0
flask-apispec==0.11.4

# API documentation
apispec==6.3.0
marshmallow==3.21.0

# Supabase
supabase==2.1.0

# Environment variables
python-dotenv==1.0.1

# Date handling
# Removed python-dateutil (unused in codebase)

# Utilities
requests==2.32.3

# Email
flask-mail==0.9.1

# NOTE: All scientific packages (numpy, scipy, scikit-learn, pandas, matplotlib, seaborn, pillow, openpyxl, XlsxWriter, reportlab, pdfkit, rdkit) are managed via conda in environment.yml only.