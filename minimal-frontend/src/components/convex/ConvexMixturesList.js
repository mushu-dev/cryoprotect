import React from 'react';

/**
 * Placeholder ConvexMixturesList component
 * Convex integration is disabled in this minimal deployment
 */
export default function ConvexMixturesList() {
  return (
    <div className="card">
      <p>Convex integration is disabled in this minimal deployment.</p>
    </div>
  );
}